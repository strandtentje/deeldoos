{
	// Plugin-files to load
	plugins = [
		f"bin/Auth.dll", f"bin/BasicHttpServer.dll", f"bin/Databases.dll", f"bin/Duckling.dll", f"bin/FlowOfOperations.dll", 
		f"bin/InputProcessing.dll", f"bin/Navigation.dll", f"bin/Networking.dll", f"bin/OutputComposing.dll", f"bin/Filesystem.dll"
	];

	// SFC-trees to load
	instances = {
		deeldoos = HttpService(prefixes = ["http://*:8000/"]) {
			http_branch = Module(
				file = f"logic.conf",
				branch = "deeldoos");
		};
	};
}
