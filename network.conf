{
	instances = {
		#include f"settings.conf"

		beacon = UdpAdvertiser(request = "name", response = instances.settings.nickname);

		network = Template(templatefile = f"templates/network.html") {
			clients_branch = UdpQuerier(querytext = "name") {
				iterator_branch = Template(templatefile = f"templates/networknode.html");
			};
		};
	};
}